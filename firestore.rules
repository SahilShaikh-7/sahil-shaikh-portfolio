rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No public read/write to messages
    match /messages/{docId} {
      allow read, write: if false;
    }
    // Lock down everything else by default
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
